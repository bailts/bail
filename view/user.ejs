<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/static/styles/main.css"/>
    <title>Pengguna - Bail</title>
</head>
<body>
    <%-include('components/nav')%>
    <div class="pt-12 pl-48">
        <div class="p-4">
            <div class="flex">
                <h1 class="font-normal text-grey-darkest flex-grow">Pengguna</h1>
                <input class="border p-2 rounded"/>
                <button class="py-2 px-4 bg-green text-white rounded ml-2">Cari</button>
                <button class="py-2 px-4 bg-green text-white rounded ml-2" onclick="location.href='/user/new'">Buat Baru</button>
            </div>
            <div class="mt-4">
                <ul class="list-reset">
                    <%users.forEach((e) =>
                    {%>
                    <li class="my-2 p-4 rounded-lg border">
                        <a class="no-underline text-black" href="/mail/1">
                            <span class="text-xl block text-black"><%=e.fullname%></span>
                            <span class="block text-sm mt-1 text-grey-darker"><span class="font-bold"><%=e.username%></span> · <%=(e.level == 1) ? 'Karyawan' : 'Admin'%></span>
                            <div class="mt-3">
                                <a href="/user/<%=e.id%>/edit" class="no-underline mr-2 text-blue">Ubah</a>
                                <a href="/api/user/<%=e.id%>/delete?confirm=false" class="no-underline ml-2 text-blue">Hapus</a>
                            </div>
                        </a>
                    </li>
                    <%})%>
                </ul>
            </div>
        </div> 
    </div>
</body>
</html>