<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/static/styles/main.css"/>
    <title><%=(operation == 'Tambah') ? 'Surat Baru' : 'Mengedit x'%> - Bail</title>
</head>
<body>
    <%-include('components/nav')%>
    <div class="pt-12 pl-48">
        <div class="p-4">
            <form>
                <input class="focus:outline-none pb-2 border-b font-normal text-grey-darkest text-4xl" value="<%=(operation != 'Tambah') ? data.mailSubject : ''%>" placeholder="Subjek"/>
                <input class="focus:outline-none pb-2 border-b font-normal text-grey-darker mt-1 block text-sm" value="<%=(operation != 'Tambah') ? data.mailCode : '' %>" placeholder="Kode Surat"/>
                <span class="font-normal text-grey-dark mt-4 block">Dibuat <input type="date" value="<%=(operation != 'Tambah') ? moment(data.mailDate).format('YYYY-MM-DD') : '' %>"/>, Diterima <input type="date" value="<%=(operation != 'Tambah') ? moment(data.incomingAt).format('YYYY-MM-DD') : '' %>"/></span>
                <span class="font-normal text-grey-darker mt-1 block">Dikirim oleh <input class="font-bold text-grey-darker focus:outline-none pb-2 border-b" value="<%=(operation != 'Tambah') ? data.mailFrom : '' %>" placeholder="Dari"/> untuk <input class="font-bold text-grey-darker focus:outline-none pb-2 border-b" value="<%=(operation != 'Tambah') ? data.mailTo : '' %>" placeholder="Tujuan"/></span>
                <span class="font-normal text-grey-darker mt-1 block">Tipe Surat: <select class="font-bold"><option>Pemberitahuan</option></select></span>
                <span class="font-normal text-grey-darker mt-1 block">Diarsipkan Oleh: <span class="font-bold"><%=(operation != 'Tambah') ? `Wibutachi` : '' %></span></span>
                <h2 class="mt-4 font-normal text-grey-darkest text-2xl">Deskripsi</h2>
                <textarea class="mt-2 w-full h-48 focus:outline-none pb-2 border-b"><%=(operation != 'Tambah') ? data.description : '' %></textarea>
                <div class="mt-2">Surat: <input type="file"/></div>
                <%if(operation == 'Tambah'){%>
                <input type="hidden" value="<%=to%>" name="target"/>
                <%}%>
                <input class="mt-2 p-4 bg-green text-white rounded" type="submit" value="Simpan"/>
            </form>
        </div> 
    </div>
</body>
</html>