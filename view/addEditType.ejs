<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/static/styles/main.css"/>
    <title><%=operation%> Tipe Surat - Bail</title>
</head>
<body>
    <%-include('components/nav')%>
    <div class="pt-12 pl-48">
        <div class="p-4">
            <div class="flex">
                <h1 class="font-normal text-grey-darkest flex-grow"><%=operation%> Tipe</h1>
            </div>
            <div class="mt-4">
                <form method="POST" action="/api/mailType/<%=(operation == 'Tambah') ? 'new' : data.id + '/update'%>">
                    <label class="block">Nama Tipe</label>
                    <input class="mt-4 border rounded p-2" name="type" type="text" value="<%=(operation != 'Tambah')  ? data.type : ''%>" required/>
                    <input class="block mt-4 py-2 px-4 rounded bg-green text-white" value="Simpan" type="submit"/>
                </form>
            </div>
        </div> 
    </div>
</body>
</html>