const config = {
    host: "localhost",
    database: "bail",
    admin: {
        user: "admin",
        password: "adm00n"
    },
    employee: {
        user: "employee",
        password: "empl00yee"
    }
}

module.exports = config